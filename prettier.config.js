/**
 * Formatting for the whole repo. Mirrors dr-pam's `shared-config/prettier.config.js` so code moving
 * between the two projects does not reflow.
 */
const config = {
	semi: true,
	trailingComma: 'all',
	singleQuote: true,
	printWidth: 120,
	tabWidth: 4,
	useTabs: true,
};

export default config;
