// Palate attribute keys. Plain data — see the note in noseFields.ts.

// The structural calls the phase requires. Texture and Complexity are recorded
// but optional, so they are not part of completion.
export const PALATE_REQUIRED = ['Sweetness', 'Acid', 'Tannin', 'Alcohol', 'Body', 'Finish'];
